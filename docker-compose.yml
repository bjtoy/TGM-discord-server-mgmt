version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  dashboard:
    build: ./dashboard
    ports:
      - "8080:80"
    restart: unless-stopped
